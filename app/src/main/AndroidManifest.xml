<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Доступ к /sdcard/AutoResponder (about.md, бэкапы БД). Грант выдаёт модуль (appops). -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Голосовой автоответчик: авто-ответ/отбой без роли дефолтной звонилки, мьют звука,
         и foreground-сервис на время обработки звонка. ANSWER_PHONE_CALLS грантит модуль. -->
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_action_call"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoResponder">

        <activity android:name=".ui.HistoryActivity" android:exported="false" />
        <activity android:name=".ui.AmRecordingsActivity" android:exported="false" />
        <activity android:name=".ui.BlacklistActivity" android:exported="false" />
        <activity android:name=".ui.HistoryChatActivity" android:exported="false" />
        <activity android:name=".ui.AppPickerActivity" android:exported="false" />
        <activity android:name=".ui.StatsActivity" android:exported="false" />
        <activity android:name=".ui.StatusActivity" android:exported="false" />
        <activity android:name=".ui.InboxActivity" android:exported="false" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Call screening: получает номер до звонка, отклоняет и триггерит SMS -->
        <service
            android:name=".call.CallScreeningServiceImpl"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- Голосовой автоответчик: обрабатывает один звонок (ответ, приветствие, запись). -->
        <service
            android:name=".call.AnswerMachineService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="voice_answering_machine" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver android:name=".notif.NotifActionReceiver" android:exported="false" />
        <receiver android:name=".sms.SmsSentReceiver" android:exported="false" />

        <!-- Снять карточку поверх экрана, когда звонок закончился -->
        <receiver android:name=".call.CallStateReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Кнопки карточки звонящего: спросить и отправить статус клиенту -->
        <receiver android:name=".notif.CallerCardReceiver" android:exported="false" />

        <!-- Ручной тест блокировки экрана/тача автоответчика без реального звонка (adb am start) -->
        <activity
            android:name=".call.AmTestActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <!-- adb-импорт аудиофайла приветствия/hold-лупа в обход UI (см. ImportAudioActivity) -->
        <activity
            android:name=".ui.ImportAudioActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <!-- adb-переключение частых булевых настроек в обход UI (см. SetFlagActivity) -->
        <activity
            android:name=".ui.SetFlagActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <!-- Ежедневный бэкап БД истории (+ перепланирование после перезагрузки) -->
        <receiver android:name=".store.BackupReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Приём входящих SMS -->
        <receiver
            android:name=".sms.SmsReceiver"
            android:permission="android.permission.BROADCAST_SMS"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Слушатель уведомлений (WhatsApp/Telegram/RCS) — заготовка, логика позже -->
        <service
            android:name=".notif.NotifListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
